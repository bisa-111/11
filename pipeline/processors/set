设置字段值。如果字段不存在则创建；如果存在则覆盖。
        ```json
        {
          "set": {
            "description": "Set the status field to active",
            "field": "status",
            "value": "active"
          }
        }
