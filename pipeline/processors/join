将数组字段的所有元素用分隔符连接成一个字符串。
        ```json
        {
          "join": {
            "field": "tag_array",
            "separator": "|"
          }
        }
        ```
           `["a", "b", "c"]` -> `"a|b|c"`
